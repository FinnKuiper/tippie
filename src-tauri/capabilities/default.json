{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities available to the main AcademicWrite window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
